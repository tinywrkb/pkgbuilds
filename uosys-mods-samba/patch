--- a/etc/samba/smb.conf
+++ b/etc/samba/smb.conf
@@ -1,3 +1,6 @@
+# smb.conf.default example from
+#   https://github.com/samba-team/samba/blob/master/examples/smb.conf.default
+#
 # This is the main Samba configuration file. You should read the
 # smb.conf(5) manual page in order to understand the options listed
 # here. Samba has a huge number of configurable options (perhaps too
@@ -23,10 +26,10 @@
 [global]
 
 # workgroup = NT-Domain-Name or Workgroup-Name, eg: MIDEARTH
-   workgroup = MYGROUP
+   workgroup = samba-private
 
 # server string is the equivalent of the NT Description field
-   server string = Samba Server
+   server string = Samba Server on %h
 
 # Server role. Defines in which mode Samba will operate. Possible
 # values are "standalone server", "member server", "classic primary
@@ -52,7 +55,7 @@
 
 # this tells Samba to use a separate log file for each machine
 # that connects
-   log file = /usr/local/samba/var/log.%m
+   log file = /var/log/samba/%m.log
 
 # Put a capping on the size of the log files (in Kb).
    max log size = 50
@@ -108,6 +111,12 @@
 ;  delete user from group script = /usr/sbin/deluser %u %g
 ;  delete group script = /usr/sbin/groupdel %g
 
+# service inherited settings
+   # this keeps auto home dirs visible while the homes share can still be hidden
+   browseable = yes
+   # https://wiki.samba.org/index.php/Setting_up_a_Share_Using_POSIX_ACLs#Making_Files_Executable
+   acl allow execute always = yes
+
 
 #============================ Share Definitions ==============================
 [homes]
@@ -118,7 +127,7 @@
 # Un-comment the following and create the netlogon directory for Domain Logons
 ; [netlogon]
 ;   comment = Network Logon Service
-;   path = /usr/local/samba/lib/netlogon
+;   path = /srv/smb/netlogon
 ;   guest ok = yes
 ;   writable = no
 ;   share modes = no
@@ -127,7 +136,7 @@
 # Un-comment the following to provide a specific roving profile share
 # the default is to use the user's home directory
 ;[Profiles]
-;    path = /usr/local/samba/profiles
+;    path = /srv/smb/profiles
 ;    browseable = no
 ;    guest ok = yes
 
@@ -136,7 +145,7 @@
 # specifically define each individual printer
 [printers]
    comment = All Printers
-   path = /usr/spool/samba
+   path = /var/spool/samba
    browseable = no
 # Set public = yes to allow user 'guest account' to print
    guest ok = no
@@ -220,4 +229,14 @@
 ;   printable = no
 ;   create mask = 0765
 
+# default public share
+[public]
+   comment = Public share on %h
+   path = /srv/smb/public
+   public = yes
+   browseable = yes
+   writable = yes
+   valid users =
 
+# uosys local system config
+include = /var/etc/samba/local.conf
