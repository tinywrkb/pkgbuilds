# Maintainer: tinywrkb <tinywrkb@gmail.com>

pkgname='trxo'
pkgver=2021.03.14.1
pkgrel=1
pkgdesc='trxo: trasmission-remote xdg-open'
arch=('any')
url=''
license=('GPL2')
source=('config' 'LICENSE.md' 'README.md'
        'trxo' 'trxo.desktop' 'trxo.xml')
sha256sums=('SKIP' 'SKIP' 'SKIP'
            'SKIP' 'SKIP' 'SKIP')

package() {
  depends+=('transmission-cli')

  install -Dm755 trxo -t ${pkgdir}/usr/bin/
  install -Dm644 trxo.desktop -t ${pkgdir}/usr/share/applications/
  install -Dm644 trxo.xml -t ${pkgdir}/usr/share/mime/packages/
  install -Dm644 config -t ${pkgdir}/usr/share/doc/${pkgname}/
  install -Dm644 README.md -t ${pkgdir}/usr/share/doc/${pkgname}/
  install -Dm644 LICENSE.md -t ${pkgdir}/usr/share/licenses/${pkgname}/
}
